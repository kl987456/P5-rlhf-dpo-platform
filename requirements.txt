# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Task queue (training jobs run as Celery tasks)
celery[redis]==5.3.6
redis==5.0.4

# Database
sqlalchemy==2.0.30
alembic==1.13.1

# Training — SFT, Reward Model, DPO
torch==2.3.0
transformers==4.41.0
peft==0.11.1                 # LoRA adapters
bitsandbytes==0.43.1         # 4-bit NF4 quantization (QLoRA)
datasets==2.19.1
accelerate==0.30.0
trl==0.9.4                   # DPO trainer utilities

# HuggingFace Hub (push trained models)
huggingface_hub==0.23.2

# Numerical / GAE computation
numpy==1.26.4

# Model serving (inference.py)
sentencepiece==0.2.0         # tokenizer dependency for some models

# Validation / serialization
pydantic==2.7.1
pydantic-settings==2.2.1

# Utilities
python-dotenv==1.0.1
httpx==0.27.0
aiofiles==23.2.1
tenacity==8.3.0
loguru==0.7.2

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
